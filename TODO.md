# TODO — Замыкание контуров DXF

Рабочее ТЗ. Источник: разбор логотипа «Велес» (ARCODIM), 2026-08-08.

## Утверждённые требования

- [x] Ядро: чтение DXF (LINE, LWPOLYLINE, POLYLINE, ARC, CIRCLE, ELLIPSE,
      SPLINE, содержимое INSERT), спрямление кривых с точностью 0.01 мм.
- [x] Параметр «радиус сшивки» — на каком расстоянии считать два конца одним стыком.
- [x] Радиус применяется только к висячим концам, а не ко всей геометрии
      (иначе схлопываются короткие отрезки на остриях и рвут целый контур).
- [x] Привязка висячего конца к телу ближайшего отрезка с разрезанием (T-стык).
- [x] Сборка в замкнутые LWPOLYLINE, на развилках — продолжение с наименьшим изломом.
- [x] Слияние коллинеарных точек, допуск 0.001 мм.
- [x] Окно с картинкой сразу после открытия файла, зум колесом и панорама.
- [x] Подсветка: замкнутые синим, открытые красным, мостики оранжевым,
      висячие концы кружками.
- [x] Кнопка перелёта к очередному незакрытому месту.
- [x] Сохранение рядом с исходником, оригинал не трогается.
- [x] Запуск на Windows и macOS.
- [x] Установка одной командой с GitHub, Python на Windows ставится сам (winget).
- [x] Ярлык на рабочем столе с иконкой создаётся установщиком.
- [x] Пакетный режим без окна: `--check`, `--save`.

## Штриховка (добавлено 2026-08-08)

- [x] Порядок работы: сначала контуры собираются и замыкаются, потом отдельно
      добавляется штриховка — уже обычными линиями.
- [x] HATCH раскладывается в линии узора (`ezdxf.render.hatching`), сущность
      HATCH в результат не попадает — станок её не понимает.
- [x] Сплошная/градиентная заливка узора не имеет — сохраняем очертание пятна.
- [x] Уже расчленённая штриховка распознаётся по слою: доля отрезков, у которых
      оба конца висят сами по себе (штриховка ~100 %, контур ~0 %).
- [x] Слои в окне переключаются вручную поверх автоопределения.
- [x] Галочка «штриховку в результат» и флаги `--hatch-layer` / `--no-hatch`.

## Проверено на практике

- Логотип «Велес» (6662 отрезка, 1.3 МБ): разрывов нет, 152 замкнутых контура,
  файл 278 КБ. Периметр сохраняется с точностью 0.00003 %, максимальное
  отклонение вершины 0.001 мм.
- Знак «Югыд ва» в двух вариантах — с 37 HATCH и с расчленённой вручную
  штриховкой. Оба дают одинаковый результат по контурам: 63 замкнутых,
  16 мостиков, длина реза 9980.80 мм. До фикса вариант с расчленённой
  штриховкой давал 9612 ложных мостиков и 4519 открытых контуров.

## Не сделано намеренно

- Раздвижение слишком тонких перемычек (в «Велесе» есть места по 0.0014 мм).
  Это правка формы, а не топологии — нужен отдельный разговор о минимальной
  ширине под конкретный станок.
- Удаление наложенных друг на друга отрезков. На остриях веток контур идёт
  «туда-обратно», и удаление дубля там разрывает контур.
- Определение вложенности контуров (что внутри чего) и порядка резки.

## Идеи на потом

- [ ] Экспорт в SVG для проверки в браузере.
- [ ] Показ длины реза по каждому контуру и суммарной оценки времени.
- [ ] Drag&drop файла на окно.
- [ ] Профили радиуса под конкретные станки.
